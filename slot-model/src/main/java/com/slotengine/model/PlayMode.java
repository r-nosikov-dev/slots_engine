package com.slotengine.model;

public enum PlayMode {
    /** Regular paid spin on the base reel set. */
    NORMAL,
    /** Extra cost, denser feature reels (ante / extra bet). */
    ANTE,
    /** Feature purchase: pay a multiple of total bet, enter free spins. */
    BUY_BONUS
}
